{
  "name": "tree-hurricane-markets-mainnet-testing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TREE Hurricane Markets mainnet testing package with Sui Move contract, Netlify functions, and live NOAA/NHC data.",
  "scripts": {
    "start": "node server.mjs",
    "netlify:dev": "netlify dev",
    "check:contract": "cd contracts/tree_hurricane_markets && sui move build",
    "check:server": "node --check server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
